use flake

# Set database based on git branch (uses socket auth)
branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "main")
if [ "$branch" = "main" ]; then
  export DATABASE_URL="postgres:///djv?host=/run/postgresql"
  echo "📦 Database: djv (main branch)"
else
  export DATABASE_URL="postgres:///djv_dev?host=/run/postgresql"
  echo "🔧 Database: djv_dev (branch: $branch)"
fi

# Sync configuration
export DJV_GITHUB_USER="djvcom"
