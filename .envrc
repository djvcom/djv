use flake

# Set database based on git branch
branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "main")
if [ "$branch" = "main" ]; then
  export DATABASE_URL="postgres://localhost/djv"
  echo "📦 Database: djv (main branch)"
else
  export DATABASE_URL="postgres://localhost/djv_dev"
  echo "🔧 Database: djv_dev (branch: $branch)"
fi
